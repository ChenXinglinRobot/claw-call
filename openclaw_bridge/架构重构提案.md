# OpenClaw Bridge 架构重构与模块拆分设计方案 V2 版

## 1. 背景与重构动机

当前项目是一个结合了 OpenClaw 与豆包实时语音 API 的语音交互应用。核心网关 `openclaw_bridge.py` 承担了所有的中间件逻辑，包括飞书免登鉴权、本地沙盒文件管理、记忆水位计算与归档、以及与外部大模型网关的 HTTP 通信。

随着业务场景的扩展（例如同时支撑英语词汇学习 `vocab_project` 与长辈访谈记录 `interview_project` 两个独立项目），该文件已呈现出"上帝类（God Object）"特征。为了提升系统的可维护性、降低模块间的耦合度，并为后续接入"飞书服务端消息主动推送"等新功能提供基础，现提议对该模块进行解耦重构。

---

## 2. 重构目标

| 目标 | 说明 |
|------|------|
| **单一职责原则 (SRP)** | 将网络通信、文件 I/O、业务策略彻底分离 |
| **隔离易变与稳定逻辑** | 文件系统的读写逻辑相对稳定，而大模型的交互策略易变，需物理隔离 |
| **依赖注入 (DI)** | 通过构造函数注入依赖，便于单元测试和模块替换 |
| **并发安全** | 引入异步文件锁机制，防止多任务并发写入冲突 |
| **资源统一管理** | 共享 HTTP 连接池，避免连接泄漏 |
| **扩展性** | 支持以插拔的方式新增飞书服务端 API，且不影响现有的免登链路 |

---

## 3. 目标目录结构规范

```text
openclaw_bridge/
├── __init__.py           # 暴露对外核心接口 (CoreBridge)
├── core_bridge.py        # 门面调度层 (Facade)
├── exceptions.py         # 全局自定义异常中心
├── token_manager.py      # Token 生命周期管理器
├── feishu_client.py      # 飞书官方 API 聚合层
├── openclaw_client.py    # OpenClaw 网关通信层
├── sandbox_manager.py    # 本地文件系统操作层 (含异步锁)
└── memory_manager.py     # 会话记忆与归档策略层
```

---

## 4. 依赖关系图

```
                         ┌─────────────────┐
                         │   server.py     │
                         └────────┬────────┘
                                  │ 实例化并调用
                                  ▼
                         ┌─────────────────┐
                         │   CoreBridge    │ ← 唯一入口门面
                         └────────┬────────┘
                                  │
        ┌─────────────────────────┼─────────────────────────┐
        │                         │                         │
        ▼                         ▼                         ▼
┌───────────────┐        ┌───────────────┐        ┌───────────────┐
│ TokenManager  │        │SandboxManager │        │ FeishuClient  │
│ (共享实例)    │        │ (含文件锁)    │        │ (共享 Client) │
└───────┬───────┘        └───────┬───────┘        └───────────────┘
        │                        │
        │                        ▼
        │               ┌───────────────┐
        │               │ MemoryManager │
        │               └───────┬───────┘
        │                       │
        ▼                       ▼
┌───────────────┐        ┌───────────────┐
│ FeishuClient  │◄───────│OpenClawClient │
│ (共享 Client) │        │ (共享 Client) │
└───────────────┘        └───────────────┘

图例说明：
→ 表示依赖方向（箭头指向被依赖方）
所有 HTTP 请求通过共享的 httpx.AsyncClient 完成
```

---

## 5. 核心模块详细设计

### 5.0 `exceptions.py` (全局自定义异常中心)

**核心职责**：
- 集中管理所有模块的自定义异常类
- 彻底消除模块间的循环引用风险
- 提供统一的异常层次结构，便于上层捕获和处理

**设计说明**：

在原有架构中，`FeishuAuthException` 定义在主模块中，其他模块若需使用该异常，可能导致循环导入。V2.1 版本将所有自定义异常统一收口到 `exceptions.py`，所有模块从此处导入异常定义。

**异常类设计**：

```python
"""
OpenClaw Bridge 全局异常中心

所有模块的自定义异常统一在此定义，避免循环引用问题。
"""


class OpenClawBridgeException(Exception):
    """
    OpenClaw Bridge 基础异常类
    
    所有模块级异常应继承此类，便于上层统一捕获
    """
    pass


class FeishuAuthException(OpenClawBridgeException):
    """
    飞书授权相关异常
    
    触发场景：
    - Token 获取失败
    - 用户信息获取失败
    - 授权码换取失败
    """
    pass


class SandboxInitException(OpenClawBridgeException):
    """
    沙盒初始化相关异常
    
    触发场景：
    - 模板目录不存在
    - 沙盒目录创建失败
    - 用户状态文件损坏无法恢复
    """
    pass


class ConfigLoadException(OpenClawBridgeException):
    """
    配置加载相关异常
    
    触发场景：
    - prompt.json 解析失败
    - 必要配置字段缺失
    - 配置文件损坏
    """
    pass


class OpenClawNotifyException(OpenClawBridgeException):
    """
    OpenClaw 通知相关异常
    
    触发场景：
    - 记忆结算通知发送失败
    - HTTP 请求超时或网络错误
    """
    pass


class FileWriteException(OpenClawBridgeException):
    """
    文件写入相关异常
    
    触发场景：
    - 原子写入过程中临时文件创建失败
    - 文件权限不足
    - 磁盘空间不足
    """
    pass
```

**使用示例**：

```python
# 在其他模块中导入异常
from .exceptions import FeishuAuthException, SandboxInitException

# 抛出异常
raise FeishuAuthException(f"获取 Token 失败: {response}")

# 上层统一捕获
try:
    await bridge.authenticate_and_initialize(code)
except OpenClawBridgeException as e:
    logger.error(f"桥接层异常: {e}")
```

---

### 5.1 `sandbox_manager.py` (沙盒与文件 I/O 层)

**核心职责**：
- 接管所有的 `os`, `shutil`, `aiofiles` 操作
- 系统中唯一允许直接操作底层文件系统的模块
- 提供异步文件锁机制，确保并发写入安全
- **集中管理全局常量**（V2.1 新增）

**常量管理说明**（V2.1 新增）：

以下常量由 `SandboxManager` 集中管理，其他模块应通过 `SandboxManager` 实例访问，而非自行定义：

| 常量名 | 值 | 说明 |
|--------|-----|------|
| `DEFAULT_CONFIG` | {...} | 兜底豆包配置，当用户 `prompt.json` 损坏时使用 |
| `DEFAULT_ACTIVE_PROJECT` | `"vocab"` | 默认激活项目名称（不含 `_project` 后缀） |
| `ARCHIVE_THRESHOLD_CHARS` | `35000` | 归档水位线（字符数阈值） |

**方法签名设计**：

```python
class SandboxManager:
    """
    沙盒与文件 I/O 管理器
    
    职责：
    1. 管理用户沙盒目录结构
    2. 提供线程安全的异步文件读写接口
    3. 路径规范化与安全校验
    """
    
    def __init__(self, memory_dir: str = "memory"):
        """
        初始化沙盒管理器
        
        Args:
            memory_dir: 记忆文件存储根目录
        """
        self.memory_dir = memory_dir
        self.templates_dir = os.path.join(memory_dir, "templates")
        self.users_dir = os.path.join(memory_dir, "users")
        # 异步文件锁池：key 为文件路径，value 为 asyncio.Lock
        self._file_locks: Dict[str, asyncio.Lock] = {}
        self._lock_pool_mutex = asyncio.Lock()  # 保护锁池本身的并发访问
    
    # ============ 路径获取方法（纯函数，无副作用）============
    
    def get_user_sandbox_path(self, user_id: str) -> str:
        """获取用户沙盒根路径"""
        pass
    
    def get_user_status_path(self, user_id: str) -> str:
        """获取用户状态文件路径（status.json）"""
        pass
    
    def get_project_path(self, user_id: str, project_name: str) -> str:
        """获取用户特定项目目录路径"""
        pass
    
    def get_prompt_path(self, user_id: str, project_name: str) -> str:
        """获取用户特定项目的 prompt.json 路径"""
        pass
    
    def get_memory_log_path(self, user_id: str, project_name: str) -> str:
        """获取用户特定项目的记忆日志路径"""
        pass
    
    # ============ 目录初始化方法 ============
    
    def ensure_core_directories(self) -> None:
        """确保核心目录结构存在（同步方法，在 __init__ 中调用）"""
        pass
    
    def ensure_user_sandbox(self, user_id: str) -> bool:
        """
        确保用户沙盒存在，若不存在则从模板初始化
        
        Args:
            user_id: 飞书用户身份标识
            
        Returns:
            True 表示沙盒已就绪，False 表示初始化失败
        """
        pass
    
    def ensure_project_structure_upgraded(self, user_id: str) -> None:
        """
        确保用户所有项目的目录结构已升级到最新版本
        检查并创建：raw_archives/, episodes/, master_profile.md
        """
        pass
    
    # ============ 文件读写方法（带异步锁保护）============
    
    async def get_file_lock(self, file_path: str) -> asyncio.Lock:
        """
        获取文件专属异步锁，确保同一文件的并发写入串行化
        
        Args:
            file_path: 文件的绝对路径
            
        Returns:
            该文件对应的 asyncio.Lock 实例
        """
        pass
    
    async def read_file(self, file_path: str) -> str:
        """
        异步读取文件内容
        
        Args:
            file_path: 文件绝对路径
            
        Returns:
            文件内容（字符串）
        """
        pass
    
    async def write_file(self, file_path: str, content: str, mode: str = 'w') -> None:
        """
        异步写入文件（带锁保护 + 原子写入）
        
        原子写入策略（V2.1 新增）：
        1. 先写入 `.tmp` 临时文件
        2. 通过 `os.replace` 原子覆盖目标文件
        3. 防止写入瞬间进程崩溃导致文件损坏
        
        Args:
            file_path: 文件绝对路径
            content: 要写入的内容
            mode: 写入模式，'w' 覆盖，'a' 追加
            - 注意：'a' 追加模式不使用原子写入，直接追加
        
        Raises:
            FileWriteException: 写入失败时抛出
        """
        pass
    
    async def read_json(self, file_path: str) -> Dict[str, Any]:
        """异步读取 JSON 文件并解析为字典"""
        pass
    
    async def write_json(self, file_path: str, data: Dict[str, Any]) -> None:
        """
        异步写入字典到 JSON 文件（带锁保护 + 原子写入）
        
        原子写入策略（V2.1 新增）：
        1. 先写入 `.tmp` 临时文件
        2. 通过 `os.replace` 原子覆盖目标文件
        3. 防止写入瞬间进程崩溃导致 JSON 文件损坏
        
        Args:
            file_path: 文件绝对路径
            data: 要写入的字典数据
            
        Raises:
            FileWriteException: 写入失败时抛出
        """
        pass
    
    # ============ 工具方法 ============
    
    def normalize_project_name(self, project_name: str) -> str:
        """
        规范化项目名称：安全校验 + 自动补全后缀
        
        1. 安全校验：检查去除后缀的项目名是否仅包含字母、数字、下划线、连字符
        2. 防御路径穿越攻击
        3. 自动补全 "_project" 后缀
        """
        pass
    
    def file_exists(self, file_path: str) -> bool:
        """检查文件是否存在（同步方法）"""
        pass
    
    def move_file(self, src_path: str, dst_path: str) -> None:
        """移动文件（原子操作，同步方法）"""
        pass
```

---

### 5.2 `token_manager.py` (Token 生命周期管理器)

**核心职责**：
- 统一管理飞书应用的各类 Token
- 提供 Token 自动缓存、刷新机制
- 与具体业务逻辑解耦，便于单元测试

**多进程扩展预留说明**（V2.1 新增）：

当前采用内存缓存策略，但需做到**读写解耦**，为未来网关采用 Gunicorn/Uvicorn 多 Worker 部署时，能低成本接入 Redis 或分布式锁预留抽象空间：

| 当前实现 | 未来可替换为 |
|---------|-------------|
| `_get_cached_token()` | Redis `GET token:key` |
| `_set_cached_token()` | Redis `SET token:key value EX 7200` |
| 内存变量锁 | Redis 分布式锁 |

**方法签名设计**：

```python
class TokenManager:
    """
    飞书 Token 生命周期管理器
    
    职责：
    1. 管理应用级 tenant_access_token（需缓存和自动刷新）
    2. 管理用户级 user_access_token（通过授权码换取）
    3. 提供 Token 自动刷新机制，避免过期
    """
    
    def __init__(
        self, 
        app_id: str, 
        app_secret: str,
        http_client: httpx.AsyncClient
    ):
        """
        初始化 Token 管理器
        
        Args:
            app_id: 飞书应用 ID
            app_secret: 飞书应用密钥
            http_client: 共享的 HTTP 客户端（由 CoreBridge 注入）
        """
        self.app_id = app_id
        self.app_secret = app_secret
        self._http_client = http_client
        
        # tenant_access_token 缓存
        self._tenant_token: Optional[str] = None
        self._tenant_token_expires_at: float = 0  # 过期时间戳
    
    async def get_tenant_access_token(self) -> str:
        """
        获取 tenant_access_token（应用级）
        
        自动缓存和刷新机制：
        1. 若缓存有效（未过期），直接返回
        2. 若缓存无效或不存在，调用飞书 API 获取新 Token
        
        Returns:
            有效的 tenant_access_token
            
        Raises:
            FeishuAuthException: Token 获取失败时抛出
        """
        pass
    
    async def exchange_code_for_user_token(self, code: str) -> Dict[str, Any]:
        """
        通过授权码换取用户 access_token 和身份信息
        
        Args:
            code: 飞书免登授权码
            
        Returns:
            包含 access_token 和用户信息的字典:
            {
                "access_token": "u-xxx",
                "user_id": "xxx",
                "union_id": "xxx"
            }
            
        Raises:
            FeishuAuthException: 换取失败时抛出
        """
        pass
    
    # ============ 缓存抽象层（V2.1 新增，为多进程扩展预留）============
    
    def _get_cached_token(self) -> Optional[str]:
        """
        内部方法：从缓存获取 Token（当前为内存缓存）
        
        设计意图：
        - 当前实现：直接返回内存变量 self._tenant_token
        - 未来扩展：可替换为 Redis 获取，无需修改上层调用代码
          示例：return await redis.get("feishu:tenant_token")
        
        Returns:
            缓存的 Token 字符串，若不存在则返回 None
        """
        return self._tenant_token
    
    def _set_cached_token(self, token: str, expires_at: float) -> None:
        """
        内部方法：设置 Token 缓存（当前为内存缓存）
        
        设计意图：
        - 当前实现：直接设置内存变量
        - 未来扩展：可替换为 Redis 存储，支持多 Worker 共享
          示例：await redis.set("feishu:tenant_token", token, ex=7200)
        
        Args:
            token: 要缓存的 Token 字符串
            expires_at: 过期时间戳
        """
        self._tenant_token = token
        self._tenant_token_expires_at = expires_at
    
    async def _refresh_tenant_token(self) -> None:
        """
        内部方法：调用飞书 API 刷新 tenant_access_token
        """
        pass
```

---

### 5.3 `feishu_client.py` (飞书 API 聚合层)

**核心职责**：
- 封装所有飞书官方 API 调用
- 不包含任何文件操作或业务逻辑
- 支持免登鉴权和消息推送

**方法签名设计**：

```python
class FeishuClient:
    """
    飞书官方 API 聚合层
    
    职责：
    1. 处理所有向飞书官方 API 发起的 HTTP 请求
    2. 提供免登鉴权接口
    3. 提供消息推送接口（后续扩展）
    
    设计约束：
    该模块只面向飞书官方服务器，不了解任何关于 OpenClaw 或本地记忆文件的信息
    """
    
    def __init__(
        self, 
        token_manager: TokenManager,
        http_client: httpx.AsyncClient
    ):
        """
        初始化飞书客户端
        
        Args:
            token_manager: Token 管理器（依赖注入）
            http_client: 共享的 HTTP 客户端（依赖注入）
        """
        self._token_manager = token_manager
        self._http_client = http_client
    
    async def authenticate_user(self, code: str) -> str:
        """
        核心方法：鉴权换取用户身份标识
        
        将前端 H5 传来的免登 code 置换为 user_id 或 union_id
        
        Args:
            code: 飞书免登授权码
            
        Returns:
            用户身份标识（user_id 或 union_id）
            
        Raises:
            FeishuAuthException: 鉴权失败时抛出
        """
        pass
    
    # ============ 后续扩展：消息推送接口 ============
    
    async def send_text_message(self, user_id: str, text: str) -> bool:
        """
        发送文本消息给指定用户（后续扩展）
        
        Args:
            user_id: 接收用户的 ID
            text: 消息内容
            
        Returns:
            True 表示发送成功
        """
        pass
    
    async def send_card_message(self, user_id: str, card: Dict[str, Any]) -> bool:
        """
        发送卡片消息给指定用户（后续扩展）
        
        Args:
            user_id: 接收用户的 ID
            card: 卡片消息结构体
            
        Returns:
            True 表示发送成功
        """
        pass
```

---

### 5.4 `openclaw_client.py` (OpenClaw 网关通信层)

**核心职责**：
- 封装 OpenClaw Gateway 的通信协议
- 维护与大模型网关的连接
- 支持项目与 Skill 的动态映射（从外部配置注入）

**方法签名设计**：

```python
class OpenClawClient:
    """
    OpenClaw 网关通信层
    
    职责：
    1. 封装 OpenClaw Gateway 的 HTTP 通信
    2. 发送记忆结算通知
    3. 维护项目与 Skill 的映射关系
    
    设计约束：
    动态配置（如 PROJECT_SKILL_MAP）应作为初始化参数传入，避免硬编码
    """
    
    def __init__(
        self,
        http_client: httpx.AsyncClient,
        gateway_url: str,
        token: str,
        agent_id: str,
        project_skill_map: Optional[Dict[str, str]] = None
    ):
        """
        初始化 OpenClaw 客户端
        
        Args:
            http_client: 共享的 HTTP 客户端（依赖注入）
            gateway_url: OpenClaw Gateway URL
            token: OpenClaw Token
            agent_id: OpenClaw Agent ID
            project_skill_map: 项目与 Skill 的映射字典（可选，有默认值）
        """
        self._http_client = http_client
        self._gateway_url = gateway_url
        self._token = token
        self._agent_id = agent_id
        
        # 项目与 Skill 的映射（可从外部配置注入）
        self._project_skill_map = project_skill_map or {
            "vocab_project": "vocab-learning-planner",
            "interview_project": "elder-interview-planner"
        }
    
    async def notify_memory_settlement(
        self, 
        user_id: str, 
        project_name: str, 
        project_path: str,
        archive_info: Dict[str, Any]
    ) -> bool:
        """
        发送记忆结算通知给 OpenClaw
        
        使用 Fire-and-Forget 模式，非阻塞发送 HTTP POST 请求
        
        Args:
            user_id: 用户身份标识
            project_name: 项目名称
            project_path: 项目绝对路径（用于 OpenClaw 定位文件）
            archive_info: 归档信息字典
            
        Returns:
            True 表示通知成功
        """
        pass
    
    def get_skill_for_project(self, project_name: str) -> str:
        """
        获取项目对应的 Skill 名称
        
        Args:
            project_name: 项目名称
            
        Returns:
            对应的 Skill 名称，若未找到则返回默认值
        """
        pass
```

---

### 5.5 `memory_manager.py` (会话记忆与归档策略层)

**核心职责**：
- 执行特定的业务逻辑，如对话日志格式化、水位检测、分卷切分
- 依赖 `sandbox_manager` 提供的读写接口
- 协调归档流程并触发 OpenClaw 通知

**方法签名设计**：

```python
class MemoryManager:
    """
    会话记忆与归档策略管理器
    
    职责：
    1. 格式化并保存对话日志
    2. 检测记忆水位并触发归档
    3. 协调 OpenClaw 结算通知
    
    设计约束：
    强依赖 SandboxManager 提供的读写接口，不直接操作文件系统
    """
    
    # 归档水位线（字符数阈值）
    ARCHIVE_THRESHOLD_CHARS = 35000
    
    def __init__(
        self, 
        sandbox_manager: SandboxManager,
        openclaw_client: OpenClawClient
    ):
        """
        初始化记忆管理器
        
        Args:
            sandbox_manager: 沙盒管理器（依赖注入）
            openclaw_client: OpenClaw 客户端（依赖注入）
        """
        self._sandbox = sandbox_manager
        self._openclaw = openclaw_client
    
    async def analyze_and_save_memory(
        self, 
        user_id: str, 
        dialog_history: List[Dict[str, Any]], 
        project_snapshot: str
    ) -> asyncio.Task:
        """
        核心方法：对话日志回写与记忆更新
        
        工作流：
        1. 格式化对话日志
        2. 追加写入到 memory_log.md（通过 sandbox_manager）
        3. 检测水位并执行归档
        4. 异步通知 OpenClaw 执行记忆结算
        
        GC 防护机制（V2.1 新增）：
        - 触发 OpenClaw 异步通知时，将创建的 asyncio.Task 返回给调用方
        - 调用方（CoreBridge）负责持有强引用，防止任务被 GC 回收
        
        Args:
            user_id: 飞书用户身份标识
            dialog_history: 对话历史列表，每项包含 role 和 text 字段
            project_snapshot: 项目快照（从 SessionManager 获取，通话开始时锁定）
            
        Returns:
            asyncio.Task: 后台异步通知任务，由 CoreBridge 持有强引用
        """
        pass
    
    async def check_and_archive_memory(
        self, 
        user_id: str, 
        project_name: str
    ) -> Dict[str, Any]:
        """
        弹性水位检测与归档动作
        
        在挂断时检测 memory_log.md 的字符数：
        - 若 >= 35000 字符，触发分卷归档
        - 若 < 35000 字符，安全水位，不执行切分
        
        Args:
            user_id: 飞书用户身份标识
            project_name: 项目名称（需带 _project 后缀）
            
        Returns:
            归档信息字典：
            - {"triggered": False} 未触发归档
            - {"triggered": True, "archived_file": "raw_vol_001.md", "char_count": 52000} 触发归档
        """
        pass
    
    async def _format_dialog_history(
        self, 
        dialog_history: List[Dict[str, Any]]
    ) -> str:
        """
        内部方法：格式化对话历史为 Markdown 格式
        
        Args:
            dialog_history: 对话历史列表
            
        Returns:
            格式化后的 Markdown 字符串
        """
        pass
```

---

### 5.6 `core_bridge.py` (门面调度层)

**核心职责**：
- 作为主程序的唯一入口
- 协调上述五个子模块共同完成复杂工作流
- 管理共享资源（HTTP 连接池）
- 提供统一的初始化和关闭接口

**方法签名设计**：

```python
class CoreBridge:
    """
    OpenClaw Bridge 门面调度层
    
    职责：
    1. 作为主程序（FastAPI/Sanic 路由）的唯一入口
    2. 协调子模块完成复杂工作流
    3. 管理共享资源生命周期
    
    设计约束：
    该模块只负责"组合调用"，不应包含具体的业务逻辑
    """
    
    def __init__(
        self,
        feishu_app_id: str,
        feishu_app_secret: str,
        memory_dir: str = "memory",
        openclaw_gateway_url: Optional[str] = None,
        openclaw_token: Optional[str] = None,
        openclaw_agent_id: Optional[str] = None,
        project_skill_map: Optional[Dict[str, str]] = None
    ):
        """
        初始化核心桥接层
        
        Args:
            feishu_app_id: 飞书应用 ID
            feishu_app_secret: 飞书应用密钥
            memory_dir: 记忆文件存储根目录
            openclaw_gateway_url: OpenClaw Gateway URL（可从环境变量读取）
            openclaw_token: OpenClaw Token（可从环境变量读取）
            openclaw_agent_id: OpenClaw Agent ID（可从环境变量读取）
            project_skill_map: 项目与 Skill 的映射字典（可选）
        """
        # 从环境变量读取 OpenClaw 配置
        self._gateway_url = openclaw_gateway_url or os.getenv("OPENCLAW_GATEWAY_URL", "")
        self._openclaw_token = openclaw_token or os.getenv("OPENCLAW_TOKEN", "")
        self._agent_id = openclaw_agent_id or os.getenv("OPENCLAW_AGENT_ID", "main")
        
        # 共享 HTTP 客户端（统一管理连接池）
        self._http_client = httpx.AsyncClient(timeout=15.0)
        
        # 初始化子模块（依赖注入）
        self._sandbox = SandboxManager(memory_dir=memory_dir)
        self._token_manager = TokenManager(
            app_id=feishu_app_id,
            app_secret=feishu_app_secret,
            http_client=self._http_client
        )
        self._feishu = FeishuClient(
            token_manager=self._token_manager,
            http_client=self._http_client
        )
        self._openclaw = OpenClawClient(
            http_client=self._http_client,
            gateway_url=self._gateway_url,
            token=self._openclaw_token,
            agent_id=self._agent_id,
            project_skill_map=project_skill_map
        )
        self._memory = MemoryManager(
            sandbox_manager=self._sandbox,
            openclaw_client=self._openclaw
        )
        
        # 后台任务强引用集合（防止 GC）
        self._background_tasks: Set[asyncio.Task] = set()
    
    # ============ 核心对外接口 ============
    
    async def authenticate_and_initialize(self, code: str) -> str:
        """
        核心方法一：鉴权换取身份标识 + 初始化用户沙盒
        
        工作流：
        1. 调用 feishu_client 鉴权获取 user_id
        2. 调用 sandbox_manager 确保用户沙盒存在
        
        Args:
            code: 飞书免登授权码
            
        Returns:
            用户身份标识（user_id 或 union_id）
            
        Raises:
            FeishuAuthException: 鉴权失败时抛出
        """
        pass
    
    async def generate_doubao_config(
        self, 
        user_id: str
    ) -> Tuple[Dict[str, Any], str]:
        """
        核心方法二：生成豆包会话配置
        
        工作流：
        1. 读取用户状态获取 active_project
        2. 动态加载 prompt.json
        3. 返回配置和项目快照
        
        Args:
            user_id: 飞书用户身份标识
            
        Returns:
            元组 (config, project_name):
            - config: 完整的豆包会话配置字典
            - project_name: 当前激活的项目名称（用于状态快照）
        """
        pass
    
    async def save_dialog_and_notify(
        self, 
        user_id: str, 
        dialog_history: List[Dict[str, Any]], 
        project_snapshot: str
    ) -> None:
        """
        核心方法三：保存对话日志并通知 OpenClaw
        
        工作流：
        1. 调用 memory_manager 分析并保存记忆
        2. 接收 memory_manager 返回的 asyncio.Task
        3. 将 Task 添加到强引用集合，防止 GC 回收
        4. 绑定 done_callback，任务完成后自动清理
        
        GC 防护机制（V2.1 新增）：
        - CoreBridge 负责收集 MemoryManager 返回的 Task
        - 将 Task 放入 `_background_tasks` 强引用集合
        - 绑定 `task.add_done_callback(self._background_tasks.discard)`
        - 防止任务执行中途被 Python GC 清理
        
        Args:
            user_id: 飞书用户身份标识
            dialog_history: 对话历史列表
            project_snapshot: 项目快照
        """
        # 调用 memory_manager，接收返回的 Task
        task = await self._memory.analyze_and_save_memory(
            user_id, dialog_history, project_snapshot
        )
        
        # GC 防护：将 Task 添加到强引用集合
        if task:
            self._background_tasks.add(task)
            # 任务完成后自动从集合中移除，避免内存泄漏
            task.add_done_callback(self._background_tasks.discard)
    
    async def switch_project(self, user_id: str, project_name: str) -> bool:
        """
        切换用户的活跃项目
        
        Args:
            user_id: 飞书用户身份标识
            project_name: 目标项目名称
            
        Returns:
            True 表示切换成功，False 表示失败
        """
        pass
    
    # ============ 后续扩展：消息推送 ============
    
    async def send_text_message(self, user_id: str, text: str) -> bool:
        """
        发送文本消息给指定用户（后续扩展）
        
        Args:
            user_id: 接收用户的 ID
            text: 消息内容
            
        Returns:
            True 表示发送成功
        """
        return await self._feishu.send_text_message(user_id, text)
    
    # ============ 资源管理 ============
    
    async def close(self) -> None:
        """
        优雅关闭所有资源
        
        工作流：
        1. 等待所有后台任务完成（最多 5 秒）
        2. 关闭共享 HTTP 客户端
        """
        # 等待后台任务完成
        if self._background_tasks:
            await asyncio.wait(self._background_tasks, timeout=5.0)
        
        # 关闭 HTTP 客户端
        await self._http_client.aclose()
    
    @property
    def sandbox(self) -> SandboxManager:
        """暴露 sandbox_manager 供特殊情况使用"""
        return self._sandbox
    
    @property
    def feishu(self) -> FeishuClient:
        """暴露 feishu_client 供特殊情况使用"""
        return self._feishu
```

---

### 5.7 `__init__.py` (对外接口暴露)

```python
"""
OpenClaw Bridge 模块

提供统一的飞书鉴权、沙盒管理、记忆归档和 OpenClaw 通信能力。
"""

from .core_bridge import CoreBridge
from .feishu_client import FeishuClient
from .openclaw_client import OpenClawClient
from .sandbox_manager import SandboxManager
from .memory_manager import MemoryManager
from .token_manager import TokenManager

__all__ = [
    "CoreBridge",
    "FeishuClient",
    "OpenClawClient",
    "SandboxManager",
    "MemoryManager",
    "TokenManager",
]
```

---

## 6. 迁移与实施步骤

采用"绞杀者模式"进行渐进式迁移，确保系统始终可用：

### 第一阶段：基础设施下沉

| 步骤 | 操作 | 验证点 |
|------|------|--------|
| 1.1 | 创建 `openclaw_bridge/` 目录结构 | 目录结构正确 |
| 1.2 | 实现 `sandbox_manager.py`，迁移所有文件操作 | 单元测试通过 |
| 1.3 | 在原 `OpenClawBridge` 中实例化 `SandboxManager` 并替换旧调用 | 回归测试通过 |

### 第二阶段：网络层剥离

| 步骤 | 操作 | 验证点 |
|------|------|--------|
| 2.1 | 实现 `token_manager.py` | Token 获取和刷新正常 |
| 2.2 | 实现 `feishu_client.py`，移除沙盒初始化职责 | 免登鉴权正常 |
| 2.3 | 实现 `openclaw_client.py`，注入配置 | 通知发送正常 |
| 2.4 | 在原 `OpenClawBridge` 中集成以上模块 | 回归测试通过 |

### 第三阶段：业务逻辑重塑

| 步骤 | 操作 | 验证点 |
|------|------|--------|
| 3.1 | 实现 `memory_manager.py` | 记忆保存和归档正常 |
| 3.2 | 实现 `core_bridge.py` 作为门面 | 所有流程正常 |
| 3.3 | 更新 `server.py` 的导入和实例化方式 | 完整回归测试通过 |

### 第四阶段：功能新增与清理

| 步骤 | 操作 | 验证点 |
|------|------|--------|
| 4.1 | 在 `feishu_client.py` 中实现消息推送功能 | 新功能可用 |
| 4.2 | 删除旧的 `openclaw_bridge.py` 单文件 | 无遗留引用 |
| 4.3 | 更新文档和测试用例 | 文档同步 |

---

## 7. 风险预案

| 风险 | 应对措施 |
|------|---------|
| **并发写入冲突** | `SandboxManager` 提供异步文件锁，确保同一文件的写入串行化 |
| **HTTP 连接泄漏** | 共享 `httpx.AsyncClient`，由 `CoreBridge.close()` 统一关闭 |
| **后台任务丢失** | `CoreBridge` 维护 `_background_tasks` 强引用集合，关闭时等待完成 |
| **迁移期间双写** | 每阶段完成后进行回归测试，使用特性开关快速回滚 |

---

## 8. 与现有代码的兼容性

为确保迁移平滑，`CoreBridge` 的核心方法命名与原 `OpenClawBridge` 保持一致：

| 原 OpenClawBridge 方法 | CoreBridge 对应方法 | 变化说明 |
|----------------------|-------------------|---------|
| `authenticate_feishu_user(code)` | `authenticate_and_initialize(code)` | 新增沙盒初始化职责 |
| `generate_doubao_config(user_id)` | `generate_doubao_config(user_id)` | 签名不变 |
| `analyze_and_save_memory(...)` | `save_dialog_and_notify(...)` | 内部委托给 memory_manager |
| `switch_project(...)` | `switch_project(...)` | 签名不变 |
| `close()` | `close()` | 签名不变 |

---

**文档版本**：V2.1
**创建日期**：2026-04-06
**修订日期**：2026-04-06
**评审状态**：已通过技术评审

**V2.1 修订说明**：
- 新增 `exceptions.py` 全局异常中心，消除模块间循环引用风险
- SandboxManager 新增常量集中管理，`write_file`/`write_json` 方法强制采用原子写入策略
- TokenManager 新增缓存抽象层，为多进程扩展预留 Redis 接入空间
- 修复后台异步任务 GC 静默回收风险，MemoryManager 返回 Task，CoreBridge 持有强引用
- 统一 `ARCHIVE_THRESHOLD_CHARS = 35000`

---
